<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ileen_mobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".grade.GradeActivity"></activity>
        <activity android:name=".activity.cobaaConversationActivity" />
        <activity android:name=".practice.ResultActivity" />

        <service
            android:name=".login.MusicService"
            android:enabled="true" />

        <activity android:name=".practice.OneActivity" />
        <activity android:name=".weather.WeatherActivity" />
        <activity android:name=".transportation.TransportationActivity" />
        <activity android:name=".sport.SportActivity" />
        <activity android:name=".practice.PracticeActivity" />
        <activity android:name=".number.NumberActivity" />
        <activity android:name=".food.FoodActivity" />
        <activity android:name=".conversation.ConversationActivity" />
        <activity android:name=".animal.AnimalActivity" />
        <activity android:name=".clock.ClockActivity" />
        <activity android:name=".hobbies.HobbiesActivity" />
        <activity android:name=".login.InputNamaActivity" />
        <activity android:name=".menu.MenuActivity" />
        <activity android:name=".login.PlayActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>